package templates

templ Timeout() {
	<div class="timeout" id="secret_container" hx-swap-oob="true">
		<h2>Your session has finished.</h2>
		<p>Refresh page and use new code to confirm secret passing.</p>
	</div>
}
